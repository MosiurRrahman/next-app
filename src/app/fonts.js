import {
  Inter,
  Hanken_Grotesk,
} from "next/font/google";

export const font_hankenGrotesk = Hanken_Grotesk({
  subsets: ["latin"],
  display: "swap",
});
export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});
